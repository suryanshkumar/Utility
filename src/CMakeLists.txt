# Adds a library called Hello (libHello.a under Linux) from the source file hello.cc
include_directories(
    /usr/local/include
    /usr/include/gdal)
ADD_LIBRARY( Utility

        visionutility
	      )

